node_modules/
# Never commit any local JSON here (wallet keys, dumps). Allowlist only the
# config files; everything else, including a stray id.json/owner.json, is ignored.
*.json
!package.json
!tsconfig.json
